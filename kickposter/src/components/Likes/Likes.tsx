import React from "react";
/*
type Props = {
  count: number;
  postId: number;
};

// We can also talk about how now that we have the post id down here, we can potentially add the ability to like the post itself - it's actually a bit complex and silly given
// our local storage schema, but it helps describe why we would want a Likes component vs inline

const Likes: React.FC<Props> = (props) => {
  return <span className="Likes">&#128157; {props.count}</span>;
};


export default Likes;
*/
